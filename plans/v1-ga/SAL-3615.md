# SAL-3615: AI-W2-C external_agents table migration

## Target paths
- migrations/external_agents/001_external_agents_table.sql
- migrations/external_agents/README.md
- src/alfred_coo/agent_ingest/models.py
- src/alfred_coo/agent_ingest/repository.py
- tests/test_external_agents_repository.py
- tests/test_external_agents_migration.py

## Acceptance criteria
1. Migration applies cleanly on dev DB.
2. INSERT + SELECT round-trips for both inbound (with framework set) and outbound (with actions populated) rows.
3. Indexes show in `\d external_agents` output (postgres) or equivalent.

## Verification approach
- Run `tests/test_external_agents_migration.py` to apply migration and assert table and indexes exist.
- Run `tests/test_external_agents_repository.py` which creates inbound and outbound agents and verifies round‑trip SELECT.
- CI passes all tests.

## Risks
- Migration may fail on older PostgreSQL versions lacking `uuid-ossp`; ensure extension is available.
- JSONB fields require proper serialization; tests cover typical payloads.