# SAL-2618: Endpoint memory pull loop

## Target paths
- src/alfred_coo/fleet_endpoint/__init__.py
- src/alfred_coo/fleet_endpoint/memory_pull.py
- tests/test_fleet_memory_pull.py

## Acceptance criteria
- Two endpoints A, B; A writes `shared.*`; B's local search returns within 90s

## Verification approach
- Unit test `test_fleet_memory_pull.py` ensures `pull_memory` returns a list of records.
- Integration test will run the pull loop at 60s cadence and verify latency.

## Risks
- Network connectivity to hub may cause timeouts; implemented simple request without retry.
