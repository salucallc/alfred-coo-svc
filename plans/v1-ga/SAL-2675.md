# SAL-2675: Add tenant support to token CLI

## Target paths
- src/mcctl/commands/token.py
- src/alfred_coo/fleet_auth/__init__.py
- src/alfred_coo/fleet_auth/tenant_binding.py
- tests/fleet/__init__.py
- tests/fleet/test_tenant_auth.py

## Acceptance criteria
- The `mcctl token create` command now accepts a `--tenant` flag.
- New fleet auth package with `__init__.py` and `tenant_binding.py` created.
- Tests for tenant auth added and all pass (`ruff` + `pytest`).

## Verification approach
- Run `mcctl token create --site foo --ttl 15m --tenant bar` and verify output includes tenant.
- Execute `pytest tests/fleet` and ensure all tests pass.

## Risks
- Adding optional flag should not break existing usage.
- Placeholder tenant binding implementation may need further development before production.
