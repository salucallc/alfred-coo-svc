# AB-25: synthetic decomposition writes plan doc

## Target paths
- tests/test_decomposition_writes_plan_doc.py
- plans/v1-ga/AB-25.md

## Acceptance criteria
- Locate the orchestrator's decomposition logic (search `orchestrator.py` for `decomposition` / `child_create` / `_create_decomp_children`).
- After creating each child Linear ticket via the existing `linear_create_issue` flow, generate the corresponding `plans/v1-ga/<code-lowercase>.md` plan doc and commit it to the alfred-coo-svc repo on a feature branch named `feature/auto-plan-doc-<parent-code>`.
- Plan doc structure follows the template in the APE/V block.

## Verification approach
Added test `tests/test_decomposition_writes_plan_doc.py` to ensure plan docs are generated during decomposition.

## Risks
- None identified.