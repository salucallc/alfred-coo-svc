# AB-25: synthetic decomposition writes plan doc

## Target paths
- tests/test_decomposition_writes_plan_doc.py
- plans/v1-ga/AB-25.md

## Acceptance criteria
**A — Action:**

1. Locate the orchestrator's decomposition logic (search `orchestrator.py` for `decomposition` / `child_create` / `_create_decomp_children`).
2. After creating each child Linear ticket via the existing `linear_create_issue` flow, generate the corresponding `plans/v1-ga/<code-lowercase>.md` plan doc and commit it to the alfred-coo-svc repo on a feature branch named `feature/auto-plan-doc-<parent-code>`.
3. Plan doc structure:

   ```markdown

## Plan doc context
Plan doc (fetch via http_get): https://raw.githubusercontent.com/salucallc/alfred-coo-svc/main/plans/v1-ga/A_tiresias_in_appliance.md
Pay attention to the section matching ticket code AB-25.

## Deliverable
Open ONE PR to the target Saluca repo on a feature branch named `feature/sal-3243-<short-slug>`. Respect the APE/V block above. Keep the diff scoped to this ticket. The `## Target` block above pins the repo + paths — do NOT edit files outside those paths without opening a grounding-gap Linear issue first.
   ```
