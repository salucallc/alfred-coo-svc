# SAL-3638: Add tests for cockpit router

## Target paths
- tests/test_cockpit_router.py

## Acceptance criteria
* `/v1/cockpit/state` response includes `subagents` array
* Each entry has all fields above
* Filter excludes sessions older than 5min and non-`agent-*` sessions
* Cap at 50 entries
* No N+1 queries; one mesh_sessions read

## Verification approach
Added unit tests covering orchestrator registration, active orchestrator listing, and the `/v1/cockpit/state` endpoint.

## Risks
Minimal; only test additions.
