# SAL-3638: Extend cockpit state with subagents

## Target paths
- src/co_w2_b_extend_v1_cockpit_state_with_act.py

## Acceptance criteria
* `/v1/cockpit/state` response includes `subagents` array
* Each entry has all fields above
* Filter excludes sessions older than 5min and non-`agent-*` sessions
* Cap at 50 entries
* No N+1 queries; one mesh_sessions read

## Verification approach
- Unit tests in `tests/test_cockpit_state.py` verify filtering, cap, and required fields.
- Manual `curl` request to `/v1/cockpit/state` confirms JSON shape and limits.

## Risks
- Assumes `get_mesh_sessions` returns `created_at` as timezone‑aware `datetime` objects.
- Large session tables may impact performance; pagination not implemented.
