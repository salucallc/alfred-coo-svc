# SAL-2629: E2E fleet integration test (F21)

## Target paths
- tests/fleet/__init__.py
- tests/fleet/e2e/__init__.py
- tests/fleet/e2e/test_e2e_fleet.py
- tests/fleet/e2e/docker-compose.fleet-e2e.yml
- tests/fleet/e2e/conftest.py
- .github/workflows/fleet_e2e.yml

## Acceptance criteria
```
3 endpoints on 3 Docker networks; task-completion + memory-replication + blackout-recovery suites pass; JUnit XML; CI green
```

## Verification approach
- Run the pytest suite with `--junitxml` to produce JUnit XML.
- CI workflow `fleet_e2e.yml` ensures the suite runs on every PR and must succeed.

## Risks
- Docker compose environment variables must match production expectations.
- Network isolation between endpoints is simulated; may need adjustments for real environments.
