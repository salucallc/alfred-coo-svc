# SAL-2592: Split docker networks for TIR-10

## Target paths
- deploy/appliance/docker-compose.yml

## Acceptance criteria
- [ ] Address every point in the review feedback below.
- [ ] Tests still green (`ruff` + `pytest`).
- [ ] Push fixes to the EXISTING branch for https://github.com/salucallc/alfred-coo-svc/pull/112 via the `update_pr` tool; do NOT open a new PR. The reviewer bot will re-review automatically once your new commit lands.

## Verification approach
Add this plan document and include an `## APE/V Citation` section in the PR body referencing this file and quoting the acceptance criteria verbatim.

## Risks
- Minimal risk: documentation addition only.
