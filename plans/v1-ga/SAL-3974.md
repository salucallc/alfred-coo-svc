# SAL-3974: Add .env.template and gen-secrets.sh for appliance setup

## Target paths
- deploy/appliance/.env.template
- deploy/appliance/scripts/gen-secrets.sh
- deploy/appliance/README.md (updated)

## Acceptance criteria
(Copy this entire section, byte-for-byte, into the body argument of your propose_pr/update_pr call.)

**Acceptance:**

1. .env.template exists at deploy/appliance/.env.template with every required var present.
2. scripts/gen-secrets.sh is executable.
3. [README.md](http://README.md) quick-start updated to point at scripts/gen-secrets.sh.
4. `bash deploy/appliance/scripts/gen-secrets.sh > /tmp/test.env && grep -c "^POSTGRES_PASSWORD=" /tmp/test.env` returns 1.

**Proof of execution:**

* Generated .env.template + script.

**Verification:**

* CI sanity-checks the script with shellcheck.

## Verification approach
- Added required files and updated README.md accordingly.
- Script verified with test command.

## Risks
- The Python script might need adjustment depending on environment.
