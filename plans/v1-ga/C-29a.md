# C-29A: Multi‑tenant integration assertions

## Target paths
- tests/fleet/e2e/test_multitenant.py
- plans/v1-ga/C-29a.md

## Acceptance criteria
- **APE/V Acceptance (machine‑checkable)** (verbatim):
  **A — Action:**
  
  1. Add `tests/fleet/e2e/test_multitenant.py` containing assertion class 1 + class 2:
  
     **Class 1 — Memory isolation:**
     * Configure mixed endpoint with both tenants.
     * tenant_a writes a memory `{"content": "tenant_a.secret", "topics": ["tenant_a"]}` via the existing memory-write path.
     * tenant_b runs a memory search for `tenant_a.secret`, result must contain zero rows.
     * tenant_b runs a memory search for any topic written under tenant_a, zero rows.
  
     **Class 2 — Cross‑tenant policy leakage:**
     * tenant_a config: `tool_allowlist=["mcp.github.read"]`.
     * tenant_b config: `tool_allowlist=[]`.
     * tenant_b attempts `mcp.github.read` on the same endpoint that hosts tenant_a.
     * Attempt is blocked; response error code equals exactly `tool_not_in_tenant_allowlist`.
     * Audit log row written for the blocked attempt with `tenant_id="beta-industries"` and `error_code="tool_not_in_tenant_allowlist"`.
  2. Tests must be discoverable by `pytest tests/fleet/e2e/test_multitenant.py -v`.
  3. Tests must integrate with the existing F21 harness (reuse fixtures).
  4. JUnit XML output from the F21 suite picks up the new tests under the `fleet_multitenant_*` test labels.

- **P — Plan:**
  
  * Inspect F21 harness fixtures for the standard 1‑hub‑3‑endpoints setup; extend the third endpoint to host both tenants.
  * Use the existing memory‑write/memory‑search APIs (no new endpoints).
  * Use the existing tool‑allowlist enforcement path; if it is not multi‑tenant aware, the test's failure is the proof, fix the enforcement code as part of this PR.

- **E — Evidence:**
  
  * `git diff` showing new test file + any harness fixture extensions + (if needed) enforcement code patch.
  * `pytest tests/fleet/e2e/test_multitenant.py -v` output, both test classes green.
  * JUnit XML excerpt showing the `fleet_multitenant_*` labels.

- **V — Verification (machine‑checkable):**
  
  1. File `tests/fleet/e2e/test_multitenant.py` exists.
  2. The file contains at least two pytest test functions whose names match `test_multitenant_memory_isolation*` and `test_multitenant_policy_leakage*`.
  3. `pytest tests/fleet/e2e/test_multitenant.py -v -k "memory_isolation or policy_leakage"` exits 0.
  4. JUnit XML produced by the F21 harness contains test cases tagged `fleet_multitenant_memory_isolation` and `fleet_multitenant_policy_leakage`.
  5. Audit log assertion: the test captures the audit row and asserts `error_code == "tool_not_in_tenant_allowlist"`.

## Verification approach
Run the full F21 CI suite; ensure the new test file is discovered, both test functions pass, and the generated JUnit XML includes entries with `fleet_multitenant_memory_isolation` and `fleet_multitenant_policy_leakage` labels.

## Risks
- If the existing tool‑allowlist enforcement is not tenant‑aware, the test will initially fail; the PR includes a minimal code fix to scope the allowlist check by tenant.
- Adding a second tenant to the third endpoint may require fixture adjustments; those are included in the test module setup.
