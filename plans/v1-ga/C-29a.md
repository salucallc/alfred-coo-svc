# SAL-3074 (C-29A): Phase-B decomposition child of C-29 (SAL-2677) → SAL-3074; creates `tests/fleet/e2e/test_multitenant.py`

## Target paths
- tests/fleet/e2e/test_multitenant.py
- plans/v1-ga/C-29a.md

## Acceptance criteria
- File `tests/fleet/e2e/test_multitenant.py` exists.
- The file contains at least two pytest test functions whose names match `test_multitenant_memory_isolation*` and `test_multitenant_policy_leakage*`.
- `pytest tests/fleet/e2e/test_multitenant.py -v -k "memory_isolation or policy_leakage"` exits 0.
- JUnit XML produced by the F21 harness contains test cases tagged `fleet_multitenant_memory_isolation` and `fleet_multitenant_policy_leakage`.
- Audit log assertion: the test captures the audit row and asserts `error_code == "tool_not_in_tenant_allowlist"`.

## Verification approach
Run the new tests via the existing F21 harness; ensure they appear in the JUnit XML with the expected labels and pass.

## Risks
- Placeholder implementations may need to be replaced with real integration logic before CI passes.
- Ensuring the test file is correctly discovered by the harness may require fixture adjustments.
