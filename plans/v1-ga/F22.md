# F22: Quarantine state

## Target paths
- src/alfred_coo/fleet_endpoint/quarantine.py
- src/mcctl/commands/quarantine.py
- tests/test_fleet_quarantine.py
- plans/v1-ga/F22.md

## Acceptance criteria
- Force api_key expiry during severe-degraded; endpoint enters `mode_state=quarantine`; recovery via `mcctl endpoint unquarantine`.

## Verification approach
Unit test ``tests/test_fleet_quarantine.py`` creates a dummy endpoint, triggers ``expire_api_key`` and checks that ``mode_state`` becomes ``quarantine``, then calls ``unquarantine`` and verifies it returns to ``normal``.

## Risks
None beyond typical code correctness concerns.
