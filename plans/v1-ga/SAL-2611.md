# SAL-2611: mcctl token create

## Target paths
- src/mcctl/__init__.py
- src/mcctl/__main__.py
- src/mcctl/commands/__init__.py
- src/mcctl/commands/token.py
- tests/test_mcctl_token.py

## Acceptance criteria
- `mcctl token create --site acme-sfo --ttl 15m` prints one-shot; DB row created; TTL enforced

## Verification approach
- Unit test `tests/test_mcctl_token.py` asserts the generated token string contains the site and TTL.

## Risks
- Ensure the placeholder token format does not clash with existing token IDs.
