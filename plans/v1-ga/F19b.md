# F19B: mcctl push-persona command + heartbeat reconciliation assertion

**Parent:** F19 (mcctl push commands) — see `plans/v1-ga/F19.md`
**Linear:** SAL-3071
**Wave:** 3

## Context

Sibling of F19A. This ticket implements `mcctl push-persona` and the parent's headline assertion: "endpoint heartbeat shows persona_version match within 60s". Reuses the `_upload_bundle` helper from F19A.

## APE/V Acceptance

**A â€” Action:**

1. Add `mcctl push-persona <bundle-path>` subcommand. Bundle contains `manifest.yaml` (`persona_version`, `name`, `agent_class`), persona prompt files, optional skill manifests.
2. The command:
   * Validates bundle, computes SHA256, POSTs to `POST /v1/fleet/persona-bundles`.
   * Hub persists a row in `fleet_persona_versions`.
   * Hub broadcasts the new version into the existing endpoint dispatch channel so endpoints see it on their next heartbeat.
   * Command prints `pushed persona <name>@<version> sha256=<hash>` and exits 0.
3. Endpoint heartbeat payload must include `persona_version` claim (extend the existing heartbeat model if needed).
4. Add `tests/fleet/e2e/test_push_persona_heartbeat.py` E2E test:
   * Spin up 1 hub + 1 endpoint (or use existing F21 harness fixtures).
   * Push persona v1.
   * Poll endpoint heartbeat for up to 60s; assert `persona_version == "v1"` within 60s.
   * Push v2; assert `persona_version` flips to `v2` within 60s of push.

**P â€” Plan:**

* Reuse `_upload_bundle` from F19A.
* If the heartbeat model lacks `persona_version`, extend it as part of this PR (additive-only schema change).
* Use the F21 harness fixture for the 1-hub-1-endpoint setup.
* Use a `tenacity.retry(stop=stop_after_delay(60))` loop or equivalent for the heartbeat poll.

**E â€” Evidence:**

* `git diff` of new command file, hub handler, heartbeat schema patch, tests.
* `pytest tests/fleet/e2e/test_push_persona_heartbeat.py -v` green.
* Captured heartbeat poll log showing pre-push absence and post-push presence within 60s.

**V â€” Verification (machine-checkable):**

1. `mcctl push-persona --help` exits 0 and shows the bundle-path positional arg.
2. File `tests/fleet/e2e/test_push_persona_heartbeat.py` exists with at least one test asserting heartbeat reconciliation within 60s.
3. `pytest tests/fleet/e2e/test_push_persona_heartbeat.py` exits 0.
4. Hub route `POST /v1/fleet/persona-bundles` is reachable in OpenAPI.
5. Endpoint heartbeat JSON payload includes `persona_version` field (verified by `jq '.persona_version' heartbeat.json` returning a non-null value).
