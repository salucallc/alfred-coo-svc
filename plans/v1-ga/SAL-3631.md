# SAL-3631: Add smoke test fixture for 4-plugin parity with manifest ingest

## Target paths
- tests/smoke/agent_ingest/__init__.py
- tests/smoke/agent_ingest/conftest.py
- tests/smoke/agent_ingest/test_4_plugin_parity.py
- tests/smoke/agent_ingest/fixtures/mock_alfred_coo.py
- tests/smoke/agent_ingest/fixtures/mock_soul_svc.py
- tests/smoke/agent_ingest/fixtures/mock_langchain_agent.py
- tests/smoke/agent_ingest/fixtures/mock_autogen_agent.py
- tests/smoke/agent_ingest/fixtures/mock_slack_workspace.py
- tests/smoke/agent_ingest/fixtures/mock_mcp_tool_surface.py
- tests/smoke/agent_ingest/fixtures/inbound_manifest.yaml
- tests/smoke/agent_ingest/fixtures/outbound_manifest.yaml
- .github/workflows/agent_ingest_smoke.yml

## Acceptance criteria
**Acceptance:**

1. All 6 entities (4 plugin-mode + 2 manifest) register cleanly.
2. Each gets dispatched-to (inbound) or has an action issued (outbound) with correct audit emission.
3. MSSP-federation compose: MSSP cockpit shows all 6 entities (read-only); MSSP issues 1 dispatch + 1 outbound action; both succeed with dual-signed audit.
4. Soulkey rotation across all 6 entities completes without disruption.
5. Unregister cleans up state for all 6.

**Proof of execution:**

* Smoke fixture in `tests/integration/agent-ingest-smoke/`.
* CI run report.
* Wall-clock <15 minutes for full smoke.

**Verification:**

* Smoke runs green on three consecutive CI runs.
* Failure modes (mock surface goes down mid-test) handled with clear error reporting, not test deadlock.

## Verification approach
The change is verified through newly added smoke tests that simulate the complete agent ingest lifecycle for various plugin and manifest configurations.

## Risks
There's a potential risk that the mock services don't perfectly replicate real-world behavior, which could lead to tests passing despite functional issues. Additionally, performance bottlenecks might occur if too many components are simulated simultaneously in the smoke tests.