# F16: Degraded‑mode tool behaviour matrix

## Target paths
- src/alfred_coo/fleet_endpoint/degraded_mode.py
- src/alfred_coo/fleet_endpoint/tool_fallback.py
- tests/test_degraded_mode_matrix.py
- plans/v1-ga/F16.md

## Acceptance criteria
- Cut hub connectivity; 8‑case degraded‑mode tool behavior matrix passes (github `cache_then_503`; linear `queue_and_drain`; local `passthrough`; reconnect drains)

## Verification approach
- Unit tests in ``tests/test_degraded_mode_matrix.py`` cover all eight cases, asserting that each tool resolves to the expected fallback behaviour.
- CI runs ``pytest`` with ``ruff`` lint; both must be green.

## Risks
- The fallback implementations are minimal; they must be replaced with production‑grade logic before GA.
- Ensure deletion limits are respected – no existing file content is removed.
