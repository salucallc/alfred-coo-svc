# F16: Degraded‑mode tool behavior matrix

## Target paths
- src/alfred_coo/fleet_endpoint/degraded_mode.py
- src/alfred_coo/fleet_endpoint/tool_fallback.py
- tests/test_degraded_mode_matrix.py
- plans/v1-ga/F16.md

## Acceptance criteria
- Cut hub connectivity; 8‑case matrix passes (github cached→503; linear enqueues; fs passthrough; reconnect drains)

## Verification approach
- Run the pytest suite `tests/test_degraded_mode_matrix.py` which exercises the eight relevant cases of the degraded‑mode matrix.
- Ensure all tests pass in CI (green `ruff` and `pytest`).

## Risks
- Incorrect fallback mapping could affect tool behavior in production degraded scenarios.
- The stub implementation is minimal; further integration work may be required when full toolchains are added.
