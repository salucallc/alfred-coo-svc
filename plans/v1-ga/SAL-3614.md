# SAL-3614: Register endpoint and agent ingest pipeline

## Target paths
- src/alfred_coo/agent_ingest/__init__.py
- src/alfred_coo/agent_ingest/register.py
- src/alfred_coo/agent_ingest/lifecycle_scheduler.py
- src/alfred_coo/agent_ingest/direction_router.py
- src/alfred_coo/agent_ingest/plugin_loader.py
- tests/test_agent_ingest_register.py
- tests/test_agent_ingest_lifecycle.py
- tests/test_agent_ingest_direction_routing.py

## Acceptance criteria
**Acceptance:**

1. Register endpoint accepts both inbound and outbound payloads; `direction` column written correctly.
2. Daemon's task router invokes `dispatch_inbound` for inbound entities and `dispatch_outbound` for outbound entities; cross-direction misroute returns 400.
3. Health-check cron runs every 30s and updates `last_health_check` column.

## Verification approach
- Unit tests in `tests/` exercise the register function, direction routing and lifecycle scheduler.
- Integration can be added later to verify end‑to‑end behavior.

## Risks
- Stub DB persistence may need replacement with real ORM in production.
- Health‑check scheduler runs indefinitely; cancelling in tests required.
