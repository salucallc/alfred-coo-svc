# SAL-2625: Endpoint reconcile on reconnect

## Target paths
- src/alfred_coo/fleet_endpoint/reconcile.py
- tests/test_fleet_reconcile.py
- plans/v1-ga/F17.md

## Acceptance criteria
- 10min blackout, 200 local + 150 hub writes; reconciles within 60s; no dups; global_seq monotonic

## Verification approach
- Unit tests in `tests/test_fleet_reconcile.py` verify that the `reconcile` function processes the buffered writes within 60 seconds, does not produce duplicate entries, and increments the global sequence monotonically.

## Risks
- Implementation is a simplified simulation; real integration may require handling actual write buffers and persistence.
