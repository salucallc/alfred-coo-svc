# SAL-2625: Endpoint reconcile on reconnect

## Target paths
- src/alfred_coo/fleet_endpoint/reconcile.py
- tests/test_fleet_reconcile.py
- plans/v1-ga/F17.md

## Acceptance criteria
- 10min blackout, 200 local + 150 hub writes; reconciles within 60s; no dups; global_seq monotonic

## Verification approach
- Unit test `tests/test_fleet_reconcile.py` ensures `reconcile` returns True.
- Integration tests will invoke the endpoint during reconnection scenarios.

## Risks
- Ensure no large deletions in existing files (deletion guardrail).
- Keep implementation minimal to avoid exceeding deletion limits.
