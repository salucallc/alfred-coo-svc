# F19A: mcctl push-policy command + version-row creation

**Parent:** F19 (mcctl push commands) — see `plans/v1-ga/F19.md`
**Linear:** SAL-3070
**Wave:** 3

## Context

F19 calls for two new mcctl subcommands: `push-policy` and `push-persona`. Both are mechanical CLI implementations on top of the fleet hub's existing version-bundle storage. This ticket covers ONLY `push-policy`. Sibling F19B covers `push-persona`. The shared bundle-upload helper should be authored here and reused by F19B.

## APE/V Acceptance

**A â€” Action:**

1. Add `mcctl push-policy <bundle-path>` subcommand. Bundle is a directory or `.tgz` containing policy files (Cedar `.cedar` or YAML).
2. The command:
   * Validates bundle structure (must contain `manifest.yaml` with `policy_version`, `name`, `description`).
   * Computes SHA256 of the canonical bundle bytes.
   * POSTs to `POST /v1/fleet/policy-bundles` on the hub with body `{name, version, sha256, payload_b64}`.
   * Hub creates a row in `fleet_policy_versions` table (id, name, version, sha256, uploaded_at, uploaded_by).
   * Command prints `pushed policy <name>@<version> sha256=<hash>` on success, exits 0.
3. Add the corresponding hub-side handler in the existing FastAPI/equivalent fleet router; persistence via existing DB session pattern in the repo.
4. Add `tests/cmd/mcctl/test_push_policy.py` covering: success path, missing-manifest rejection, hub-409-on-duplicate-version handling.

**P â€” Plan:**

* Inspect existing mcctl subcommands to mirror the CLI framework (cobra/click/argparse).
* Look for the existing version-row model on the hub side (e.g. `fleet_persona_versions` if pre-existing) to mirror schema choices.
* Author shared `_upload_bundle(bundle_type, path)` helper; sibling F19B reuses it.

**E â€” Evidence:**

* `git diff` of new command file, hub handler, DB migration (if needed), tests.
* `pytest tests/cmd/mcctl/test_push_policy.py -v` green.
* Manual smoke: `mcctl push-policy fixtures/policy-bundle-v1/` against test hub returns version row.

**V â€” Verification (machine-checkable):**

1. `mcctl push-policy --help` exits 0 and shows the bundle-path positional arg.
2. File `tests/cmd/mcctl/test_push_policy.py` exists with tests `test_push_policy_success`, `test_push_policy_missing_manifest`, `test_push_policy_duplicate_409`.
3. `pytest tests/cmd/mcctl/test_push_policy.py` exits 0.
4. New DB migration file (if any) lives under `migrations/` and creates `fleet_policy_versions` table.
5. Hub route `POST /v1/fleet/policy-bundles` is reachable in OpenAPI.
