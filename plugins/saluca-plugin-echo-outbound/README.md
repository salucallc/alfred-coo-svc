# Saluca Plugin Echo Outbound

A minimal outbound plugin that forwards the `echo.send` action to a configurable HTTP endpoint.

## Usage
```bash
pip install saluca-plugin-echo-outbound==0.1.0
```
Configure the endpoint via the plugin config and dispatch actions through the Saluca framework.
