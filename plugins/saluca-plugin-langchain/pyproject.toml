[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "saluca-plugin-langchain"
version = "0.1.0"
description = "Inbound plugin for LangChain agents"
authors = [{name = "Saluca LLC"}]
dependencies = ["langchain>=0.1,<0.2"]
