# Generated by automated builder for TIR-14
name: Tiresias Sovereignty Smoke Test

on:
  push:
    branches: [ main ]
  pull_request:
    paths:
      - .github/workflows/tiresias_sovereignty_smoke.yml
      - deploy/appliance/tiresias/**

jobs:
  smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: deploy/appliance/tiresias

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Compose
        run: |
          docker compose up -d --build

      - name: Run smoke test script
        run: ./smoke_test.sh

      - name: Run audit chain walk test
        run: |
          python -m pytest test_audit_chain_walk.py
