# Model pricing table
# Ollama Max = flat $100/mo amortized
ollama_max:
  flat_monthly_usd: 100

openrouter/free:
  input_per_1k: 0.0
  output_per_1k: 0.0

openrouter/paid:
  input_per_1k: 0.015
  output_per_1k: 0.02
