"""Canonical persona-move fixtures.

Each ``M-MV-*.json`` file is a single atomic behaviour drawn from Plan M §2's
taxonomy. Fixtures are schema-validated at load time via
``alfred_coo.benchmark.schema.Fixture.from_json``.
"""
