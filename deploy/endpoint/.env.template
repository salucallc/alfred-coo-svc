# Endpoint environment template
# Registration token generated on the hub (one-time use)
REGISTRATION_TOKEN=YOUR_REGISTRATION_TOKEN_HERE
# Hub base URL for registration and websocket connection
HUB_URL=wss://your-hub.example/v1/fleet/link
# Optional: path to local data directory for soul-lite
DATA_DIR=./data
