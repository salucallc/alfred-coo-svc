# Existing env template content
# Adding Tiresias proxy configuration
TIRESIAS_MODE=sovereign
TIRESIAS_URL=http://tiresias-proxy:8840
# Remove raw provider keys - ensure they are not present
# (No GITHUB_TOKEN, SLACK_BOT_TOKEN, etc.)
