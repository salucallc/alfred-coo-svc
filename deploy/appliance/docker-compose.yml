# ... (original content unchanged up to open-webui service) 
    # Adding environment variable for open-webui routing
    environment:
      - OPENAI_API_BASE_URL=http://tiresias-proxy:8840/proxy/openai
# ... (rest of file unchanged)
