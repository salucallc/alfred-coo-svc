## Acceptance (APE/V)
- Service builds Docker image and starts successfully.
- `/healthz` endpoint returns `{"status": "ok"}`.
- Tests pass (`ruff` + `pytest`).
- PR body includes citation to this APE/V document.
